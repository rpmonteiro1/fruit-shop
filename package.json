{
  "name": "fruit-shop",
  "version": "2.0.0",
  "main": "index.js",
  "license": "UNLICENSED",
  "scripts": {
    "start": "",
    "heroku-prebuild": "",
    "heroku-postbuild": "cd client && yarn && yarn build && cd ../server && yarn && DATABASE_URL=$DATABASE_URL db-migrate up",
    "deploy": ""
  },
  "engines": {
    "node": "8.1.2",
    "yarn": "0.24.5"
  }
}
